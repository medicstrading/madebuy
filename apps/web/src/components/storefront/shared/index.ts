/**
 * Shared Storefront Components
 *
 * These components are used across all tenant storefronts
 * and are configurable via the tenant's websiteDesign settings.
 */

export { Header } from './Header'
export { Footer } from './Footer'
